---
phase: 10-structs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/compiler/opcode.h, src/ast/ast.h, src/lexer/lexer.h, src/lexer/token.h, src/lexer/token.cpp]
autonomous: true
---

## Objective

Add foundation: new opcodes, AST nodes, lexer tokens, and keywords. No runtime behavior yet — just declarations that later plans consume.

## Execution Context

@/home/dulc3/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/dulc3/.config/opencode/get-shit-done/templates/summary.md

## Context

@/home/dulc3/Documents/github/compiler/.planning/STATE.md
@/home/dulc3/Documents/github/compiler/.planning/ROADMAP.md
@/home/dulc3/Documents/github/compiler/src/compiler/opcode.h
@/home/dulc3/Documents/github/compiler/src/ast/ast.h
@/home/dulc3/Documents/github/compiler/src/lexer/lexer.h
@/home/dulc3/Documents/github/compiler/src/lexer/token.h
@/home/dulc3/Documents/github/compiler/src/lexer/token.cpp

## Must-Haves

truths:
  - "Opcode enum includes OP_NEW_STRUCT, OP_GET_FIELD, OP_SET_FIELD, OP_DEFER, OP_DEFERRED_RETURN, OP_PANIC, OP_RECOVER, OP_IS_INSTANCE, OP_INVOKE"
  - "AST nodes StructDeclStmt, StructInstantiationExpr, DeferStmt, PanicExpr, RecoverExpr, InterfaceDeclStmt exist"
  - "Lexer recognizes struct, defer, panic, recover keywords"

artifacts:
  - path: src/compiler/opcode.h
    provides: New opcode definitions
    contains: OP_NEW_STRUCT, OP_GET_FIELD, OP_SET_FIELD, OP_DEFER, OP_DEFERRED_RETURN, OP_PANIC, OP_RECOVER, OP_IS_INSTANCE, OP_INVOKE
  - path: src/ast/ast.h
    provides: New AST node declarations
    contains: StructDeclStmt, StructInstantiationExpr, DeferStmt, PanicExpr, RecoverExpr, InterfaceDeclStmt
  - path: src/lexer/token.h
    provides: New token types
    contains: STRUCT_KEYWORD, DEFER_KEYWORD, PANIC_KEYWORD, RECOVER_KEYWORD, INTERFACE_KEYWORD
  - path: src/lexer/token.cpp
    provides: Keyword mappings
    contains: struct, defer, panic, recover keyword lookups

key_links:
  - from: src/compiler/opcode.h
    to: src/compiler/compiler.cpp
    via: "opcode enum used by compiler"
  - from: src/ast/ast.h
    to: src/parser/parser.cpp
    via: "AST nodes parsed by parser"
  - from: src/lexer/token.h
    to: src/lexer/lexer.cpp
    via: "keywords recognized by lexer"

## Tasks

<task type="auto">
  <name>Task 1: Add opcodes to opcode.h</name>
  <files>src/compiler/opcode.h</files>
  <action>
    Add these opcodes to the enum (insert before OP_END):
    
    // Structs
    OP_NEW_STRUCT,           // Pop n values, pop n (field count), create struct, push struct
    OP_GET_FIELD,            // Pop field name index, pop struct, push field value
    OP_SET_FIELD,            // Pop value, pop field name index, pop struct, set field
    
    // Defer
    OP_DEFER,                // Pop callable, push to defer stack
    OP_DEFERRED_RETURN,      // Execute deferred calls in LIFO order
    
    // Panic/Recover
    OP_PANIC,                // Pop value, start panic unwinding
    OP_RECOVER,              // Return panic value or undefined
    
    // Interfaces
    OP_IS_INSTANCE,          // Pop interface name index, pop value, push bool
    OP_INVOKE,               // Pop method name index, pop args, pop receiver, call method
    
    Then add them to opcodeName() switch statement using the OP() macro pattern.
  </action>
  <verify>grep -c "OP_NEW_STRUCT\|OP_GET_FIELD\|OP_SET_FIELD\|OP_DEFER\|OP_DEFERRED_RETURN\|OP_PANIC\|OP_RECOVER\|OP_IS_INSTANCE\|OP_INVOKE" src/compiler/opcode.h | grep -q "18"</verify>
  <done>9 new opcodes in enum + 9 entries in opcodeName() switch</done>
</task>

<task type="auto">
  <name>Task 2: Add AST nodes to ast.h</name>
  <files>src/ast/ast.h</files>
  <action>
    Add forward declarations (after class ThrowStmt):
    
    class StructDeclStmt;
    class StructInstantiationExpr;
    class DeferStmt;
    class PanicExpr;
    class RecoverExpr;
    class InterfaceDeclStmt;
    
    Add to Visitor interface:
    virtual void visitStructDeclStmt(StructDeclStmt* stmt) = 0;
    virtual void visitStructInstantiationExpr(StructInstantiationExpr* expr) = 0;
    virtual void visitDeferStmt(DeferStmt* stmt) = 0;
    virtual void visitPanicExpr(PanicExpr* expr) = 0;
    virtual void visitRecoverExpr(RecoverExpr* expr) = 0;
    virtual void visitInterfaceDeclStmt(InterfaceDeclStmt* stmt) = 0;
    
    Add to StatementVisitor interface:
    virtual void visitStructDeclStmt(StructDeclStmt* stmt) = 0;
    virtual void visitDeferStmt(DeferStmt* stmt) = 0;
    virtual void visitInterfaceDeclStmt(InterfaceDeclStmt* stmt) = 0;
    
    Add expression visitor declarations:
    virtual void visitStructInstantiationExpr(StructInstantiationExpr* expr) = 0;
    virtual void visitPanicExpr(PanicExpr* expr) = 0;
    virtual void visitRecoverExpr(RecoverExpr* expr) = 0;
    
    Then add full class definitions (before Program struct):
    
    class StructDeclStmt : public Stmt {
        std::string name;
        std::vector<std::pair<std::string, Type>> fields;
        // constructor, accept(), toString()
    };
    
    class StructInstantiationExpr : public Expr {
        std::string name;
        struct Field { std::string name; Expr::Ptr value; };
        std::vector<Field> fields;
        // constructor, accept(), toString()
    };
    
    class DeferStmt : public Stmt {
        Expr::Ptr expression;
        // constructor, accept(), toString()
    };
    
    class PanicExpr : public Expr {
        Expr::Ptr value;
        // constructor, accept(), toString()
    };
    
    class RecoverExpr : public Expr {
        // constructor, accept(), toString()
    };
    
    class InterfaceDeclStmt : public Stmt {
        std::string name;
        struct Method {
            std::string name;
            std::vector<std::pair<std::string, Type>> params;
            Type returnType;
        };
        std::vector<Method> methods;
        // constructor, accept(), toString()
    };
  </action>
  <verify>grep -c "StructDeclStmt\|StructInstantiationExpr\|DeferStmt\|PanicExpr\|RecoverExpr\|InterfaceDeclStmt" src/ast/ast.h | grep -q "12"</verify>
  <done>6 new AST node classes with accept() methods, 6 visitor declarations</done>
</task>

<task type="auto">
  <name>Task 3: Add keywords and tokens to lexer/token files</name>
  <files>src/lexer/lexer.h, src/lexer/token.h, src/lexer/token.cpp</files>
  <action>
    In token.h TokenType enum (after DEFAULT_KEYWORD):
    - STRUCT_KEYWORD
    - DEFER_KEYWORD
    - PANIC_KEYWORD
    - RECOVER_KEYWORD
    
    (INTERFACE_KEYWORD already exists)
    
    In token.cpp tokenTypeToString():
    Add cases:
    case TokenType::STRUCT_KEYWORD: return "struct";
    case TokenType::DEFER_KEYWORD: return "defer";
    case TokenType::PANIC_KEYWORD: return "panic";
    case TokenType::RECOVER_KEYWORD: return "recover";
    
    In token.cpp keyword table:
    map["struct"] = TokenType::STRUCT_KEYWORD;
    map["defer"] = TokenType::DEFER_KEYWORD;
    map["panic"] = TokenType::PANIC_KEYWORD;
    map["recover"] = TokenType::RECOVER_KEYWORD;
    
    In lexer.h Lexer::scanToken() switch:
    No changes needed — keywords are handled by scanIdentifier() which calls KeywordTable::get().
  </action>
  <verify>grep -q "STRUCT_KEYWORD" src/lexer/token.h && grep -q "DEFER_KEYWORD" src/lexer/token.h && grep -q "PANIC_KEYWORD" src/lexer/token.h && grep -q "RECOVER_KEYWORD" src/lexer/token.h</verify>
  <done>4 new token types, keyword mappings, toString() cases</done>
</task>

## Verification

Build: `cd build && cmake .. && make -j$(nproc)` compiles without errors.

## Success Criteria

- All 9 opcodes defined in opcode.h
- 6 new AST node classes declared and defined
- 4 new lexer keywords registered
- Project compiles cleanly

## Output

After completion, create `.planning/phases/10-structs/10-01-SUMMARY.md`
