---
phase: 10-structs
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified: [src/parser/parser.h, src/parser/parser.cpp]
autonomous: true
---

## Objective

Add parser support for struct declarations, defer statements, panic/recover expressions, interface declarations, and struct instantiation.

## Execution Context

@/home/dulc3/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/dulc3/.config/opencode/get-shit-done/templates/summary.md

## Context

@/home/dulc3/Documents/github/compiler/.planning/STATE.md
@/home/dulc3/Documents/github/compiler/.planning/ROADMAP.md
@/home/dulc3/Documents/github/compiler/src/parser/parser.h
@/home/dulc3/Documents/github/compiler/src/parser/parser.cpp
@/home/dulc3/Documents/github/compiler/src/ast/ast.h

## Must-Haves

truths:
  - "Parser recognizes `struct Name { field: Type }` syntax"
  - "Parser recognizes `defer expression;` syntax"
  - "Parser recognizes `panic expression` syntax"
  - "Parser recognizes `recover()` syntax"
  - "Parser recognizes `interface Name { method(): Type }` syntax"
  - "Parser recognizes `new Name{field: value}` syntax"

artifacts:
  - path: src/parser/parser.cpp
    provides: New parsing methods
    contains: parseStruct, parseDefer, parsePanic, parseRecover, parseInterface, parseStructInstantiation

key_links:
  - from: src/parser/parser.cpp
    to: src/ast/ast.h
    via: "parse* methods create AST nodes from Plan 01"

## Tasks

<task type="auto">
  <name>Task 1: Add parseStruct() and parseInterface()</name>
  <files>src/parser/parser.h, src/parser/parser.cpp</files>
  <action>
    In parser.h private section, add:
    std::vector<std::pair<std::string, Type>> parseFieldList();
    std::vector<std::pair<std::string, Type>> parseMethodList();
    Stmt::Ptr parseStruct();
    Stmt::Ptr parseInterface();
    
    In parser.cpp, implement:
    
    parseStruct():
    - Match STRUCT_KEYWORD
    - Parse struct name (IDENTIFIER)
    - Consume LEFT_BRACE
    - Parse field list: name : type; pairs (until RIGHT_BRACE)
    - Consume RIGHT_BRACE
    - Return StructDeclStmt
    
    parseInterface():
    - Match INTERFACE_KEYWORD (already exists)
    - Parse interface name (IDENTIFIER)
    - Consume LEFT_BRACE
    - Parse method list: name(params): returntype; pairs (until RIGHT_BRACE)
    - Consume RIGHT_BRACE
    - Return InterfaceDeclStmt
    
    parseFieldList():
    - Parse name : type; pairs until RIGHT_BRACE
    - Return vector of (name, Type)
    
    parseMethodList():
    - Parse name(params): returntype; pairs until RIGHT_BRACE
    - Return vector of Method structs
  </action>
  <verify>cd build && cmake .. && make -j$(nproc) 2>&1 | grep -q "Built target hs"</verify>
  <done>parseStruct and parseInterface parse struct/interface declarations</done>
</task>

<task type="auto">
  <name>Task 2: Add parseDefer(), parsePanic(), parseRecover()</name>
  <files>src/parser/parser.h, src/parser/parser.cpp</files>
  <action>
    In parser.h private section, add:
    Stmt::Ptr parseDefer();
    Expr::Ptr parsePanic();
    Expr::Ptr parseRecover();
    
    In parser.cpp, implement:
    
    parseDefer() — called from parseStatement():
    - Match DEFER_KEYWORD
    - Parse expression (parseExpression())
    - Consume SEMICOLON
    - Return DeferStmt
    
    parsePanic() — called from parsePrimary():
    - Match PANIC_KEYWORD
    - Parse expression (parseExpression())
    - Return PanicExpr
    
    parseRecover() — called from parsePrimary():
    - Match RECOVER_KEYWORD
    - Consume LEFT_PAREN
    - Consume RIGHT_PAREN
    - Return RecoverExpr
    
    In parseStatement(), add after RETURN_KEYWORD match:
    if (match(TokenType::DEFER_KEYWORD)) return parseDefer();
    
    In parsePrimary(), add before IDENTIFIER:
    if (match(TokenType::PANIC_KEYWORD)) return parsePanic();
    if (match(TokenType::RECOVER_KEYWORD)) return parseRecover();
  </action>
  <verify>cd build && cmake .. && make -j$(nproc) 2>&1 | grep -q "Built target hs"</verify>
  <done>parseDefer, parsePanic, parseRecover handle defer/panic/recover syntax</done>
</task>

<task type="auto">
  <name>Task 3: Add parseStructInstantiation()</name>
  <files>src/parser/parser.h, src/parser/parser.cpp</files>
  <action>
    In parser.h private section, add:
    Expr::Ptr parseStructInstantiation();
    
    In parser.cpp, implement:
    
    parseStructInstantiation():
    - Match NEW_KEYWORD
    - Parse type name (IDENTIFIER) — this is the struct type name
    - Consume LEFT_BRACE
    - Parse field-value pairs: name : expression pairs (until RIGHT_BRACE)
    - Consume RIGHT_BRACE
    - Return StructInstantiationExpr
    
    In parsePrimary(), add after NEW_KEYWORD match (before the existing new expression stub):
    return parseStructInstantiation();
    
    Replace the existing `if (match(TokenType::NEW_KEYWORD)) return nullptr;` stub.
  </action>
  <verify>cd build && cmake .. && make -j$(nproc) 2>&1 | grep -q "Built target hs"</verify>
  <done>parseStructInstantiation parses new StructName{field: value} syntax</done>
</task>

## Verification

Build: `cd build && cmake .. && make -j$(nproc)` compiles without errors.

## Success Criteria

- All 6 parsing methods implemented
- Struct, defer, panic, recover, interface, struct instantiation parse correctly
- Project compiles cleanly

## Output

After completion, create `.planning/phases/10-structs/10-02-SUMMARY.md`
